# Contaminated fixture evidence

Fixture v2 is retained as negative evidence and must not be used for scoring or promotion.

After its first complete run, paired inspection found that the expected `bidirectional` label for `bidirectional-fixed-allocation` conflicts with the registered live-state dependency contract. Changing that label after seeing model output would contaminate the evaluation further, so the original files remain byte-for-byte available here and are excluded from active gauntlet tests.
