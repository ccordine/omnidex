// Package semanticreview is an in-memory executable reference for a bounded
// semantic review and code-owned correction cycle. A station may select one
// opaque finding. Code owns finding materialization, correction derivation,
// execution, verification, recursion, bounds, and completion.
//
// The package is exercised with scripted fixtures. Its evidence is
// contaminated and is not an autonomy claim, a production cutover,
// persistence, recovery, or real inference evidence.
package semanticreview
