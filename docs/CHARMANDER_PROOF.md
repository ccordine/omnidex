# Omnidex autonomy proof ledger

Updated: 2026-09-13

Status: **Linux Go CLI functionality demonstrated; general autonomy not yet proven**

## Honest result

No current run is being claimed as proof that Omnidex can build an unfamiliar application from an ordinary request.

Earlier Go CLI runs are invalid as autonomy evidence for the current goal. They used registered application shapes and deterministic product adapters, invoked zero fragment workers, and therefore measured adapter rendering rather than general construction. Their generated projects and historical measurements must not be cited as proof of Omnidex autonomy.

The dependent simulation integration proves a narrow API integration and deterministic simulation boundary. It does not prove that Omnidex built that project.

## Linux minimal-context diagnostic — verified, 2026-09-13

The ordinary `omni chat` boundary produced a working application from a fresh
workspace using the unchanged diagnostic request below. The frozen build completed
in 293.518 seconds, with 16 local model calls, 15 successful verification commands,
and five published files. Codex approved the sole generated plan leaf without
edits and supplied no intermediate specification, correction, or source.

Code resolved input-channel and result-type semantics through separate bounded
calls. The three source calls then received only their local behavior and exact
signature: 327 bytes for the implementation, 370 for example input, and 357 for
the expected value. They received no capability declarations, paths, project
context, observed result, test methods, or workflow state. All three passed on
their first attempt. Code supplied argument passing, formatting, comparisons,
compilation, verification, and publication. Total prompt input was 13,845 bytes;
the largest intake prompt was 3,843 bytes.

Independent execution after completion returned `5` for `hello`, `3` for
`é🙂漢`, and `0` for empty text. An open Linux pseudo-terminal without EOF also
completed. Source hashes remained unchanged. In a separate copied workspace,
changing the observed integer made the code-owned comparison fail with an actual
behavior mismatch; this evaluator check never entered the production build.

This is **contaminated for autonomy qualification** because Codex selected the
diagnostic request and operated plan approval. It demonstrates the current narrow
Linux Go CLI path, not arbitrary applications or other operating systems. Broader
application construction and post-compiler semantic correction remain unqualified.

[Exact calls, process evidence, independent executions, and prior diagnostics](evidence/linux-minimal-context-success-2026-09-13.json)
are retained with adjacent raw-provider and generated-project archives.
[Framework checks](evidence/linux-minimal-context-framework-checks-2026-09-13.json)
record the full repository suite, Docker publication fixtures, live numeric/text
qualification, and the intervening failures without treating fixtures as autonomy.

## Linux functional diagnostic — independent execution passed, 2026-09-13

An ordinary `omni chat` request produced a working Go command-line application
from a fresh workspace. The frozen source snapshot completed generation,
Docker verification, and publication in 186.983 seconds, using 14 local model
calls and publishing five files. No generated source or intermediate specification
was supplied by Codex. The sole generated plan leaf was approved without edits.

Independent execution after the builder stopped confirmed argument counts of 5
for `hello`, 3 for `é🙂漢`, and 0 for empty text. An open pseudo-terminal without
EOF also completed successfully. Published source hashes remained unchanged.

The generated predicate returned constant `true`, so its passing generated tests
are not evidence of behavioral verification. The independent executions establish
only this narrow functional result. The later minimal-context diagnostic above
replaces the generated predicate with a code-owned typed comparison. It also
removes the unused input channel that caused a subsequent independently detected
failure despite passing generated tests.

This remains **contaminated for autonomy qualification** because Codex selected
the smoke request. It does establish that the Linux production request path can
produce an application that performs the requested operation. It does not qualify
arbitrary applications, other languages, or other operating systems.

[Exact calls, commands, independent executions, earlier failures, and known defect](evidence/linux-functional-success-2026-09-13.json)
are retained with the generated project archive and raw provider evidence.

## Linux ordinary CLI diagnostic — failed, 2026-09-13

The current Linux server and CLI built successfully through the supported build
scripts, including the production GUI. The actual `omni chat` terminal then ran
from a fresh directory against a dedicated fresh PostgreSQL schema and live local
Ollama models. Its captured source remained unchanged throughout execution.

The request was: “Create a small Go command-line program that counts the Unicode
characters in the text supplied as its argument.”

**Contaminated for autonomy qualification:** Codex selected this initial smoke
request. This is diagnostic evidence of the ordinary production path, excluded
from the human-request autonomy acceptance sequence. No intermediate specification,
correction, or generated file was supplied during execution.

The runtime correctly classified the request as coding. Its inventory returned
“The software counts the Unicode characters in the text supplied as its argument.”
The candidate-authorization station then answered `B`, meaning the candidate added
unrequested meaning. The candidate directly restates the requested runtime behavior;
this is an observed semantic false negative. Code discarded it and entered an empty
plan review requiring user guidance.

- Result: `waiting_input` after 20.683 seconds; no functional application.
- Live calls: 3, all `qwen3.5:9b-q4_K_M`; 5,725 total prompt bytes, 3,843 maximum.
- Source-generation calls, workload verification commands, and files written: 0.
- The CLI connected, retained the invoking directory, submitted the ordinary turn,
  and streamed real progress. Construction and publication were never reached.
- The client and dedicated server were stopped after this observation. The dedicated
  database container was removed after exporting its evidence.

[Exact call envelopes, provider responses, transcript, and stopped state](evidence/linux-functional-smoke-2026-09-13.json)
are retained. The complete source snapshot and database export remain in
`/tmp/omnidex-linux-smoke._li4gmhp`.

This failed intake run is historical. The later functional diagnostic above
reached independent Linux execution; generated-verifier quality remains a measured
limitation. Constructed fixture passes alone are not ordinary-request evidence.

## Foundation verified so far

The following checks establish framework primitives only:

* Go tests pass for portable jobs, bounded raw semantic leaves, source-span correction, AST parsing, context budgets, capability graph projection, worker routing, and workspace reconciliation.
* The generated generic browser substrate passes its real pinned Node install, Vitest suite, TypeScript type check, and Vite production build.
* That Node test caught and then guarded a real runtime defect where action status changed but state was never published.
* A historical live-core run established its then-current PostgreSQL registry and
  reported the exact active/bootstrap counts. That bootstrap path has since been
  replaced by the single destructive fresh-database setup and is not a current
  startup contract.
* PostgreSQL integration tests prove recording of actual prompt and provider-response bytes, including failed output, bounded evidence pagination, exact-input recovery, accepted-result reuse, and parent-linked corrections with an unchanged model route. No request/response or work-identity hash is required.
* Real PostgreSQL tests now verify database reads through execution, actual statement/argument/result recording, job-owned retrieval, citation checking, and exact completion replay. Two unrelated fixtures also prove that an unrelated added column does not block a read and that a missing queried column fails at PostgreSQL. These tests do not use schema, query, or result hashes.
* Real HTTP/PostgreSQL fixtures verify recorded web acquisitions, exact source excerpts, truncation, citation checks, and replay without refetching. Roleplay research fixtures additionally verify literal question binding, atomic message/citation publication, and real-world/fictional separation. Question, source, and rendered-answer hashes are not required.
* Two configured simulation fixtures verify preview rollback, actual clamped meter effects, atomic terminal publication, and replay without repeated effects, messages, or turn advancement. Request hashes have been removed from preparation, transition, and advance records; code compares the retained inputs and applied state.
* Narrative freshness tests compare actual projected values and source records, including changed persona text, exact restoration, and character-specific canon visibility. No narrative fingerprint is required. Advancement is keyed by the existing preparation; finite-use and infinite-use inventory fixtures verify stable preview/publication identity and give/take/give replay without content hashes.
* Realtime and project-map tests verify actual-content deduplication, topic-scoped bounded replay, unchanged earlier payloads, and current filesystem observations without hashed workspace identity. Server-rendered slash commands receive local DOM IDs; command text and UTF-16 insertion positions remain exact. These are UI/framework checks, not application-generation evidence.
* Two independent browser compilation fixtures verify that Unicode and long host-directory names do not become package hashes or replace the product title. The generated manifest and lock use one workspace-local technical name; the Go module compiler uses the same name. The current full Go/PostgreSQL suite passes with that boundary.
* A documentation regression test caught obsolete adapter, stack, and version-profile rows, then passed after those tables were reconciled with the executable registries. That proves the tables match registrations, not that an application succeeds.
* Seventeen database parameter and selection stations keep unrelated accepted query clauses out of their model inputs. Paired fixtures prove unchanged prompts and decoded results when unrelated accepted work changes; focused meaning and applicable choices remain visible, and invalid state still fails. Code excludes aggregates without compatible fields, already selected relations, and consumed ordering keys. Fixed-provider/PostgreSQL cases additionally verify actual prompt/response capture and accepted replay without another provider call. This is transport and state-boundary evidence, not live-model quality or autonomy proof.
* Ordinary-answer and roleplay pipeline fixtures verify separate question-relevance and factual-support inputs, candidate-local negative results, exact duplicate suppression, code-bound citations, and zero-provider-call replay. They record the actual fixed-provider requests and responses in PostgreSQL and caught a replay-accounting defect. Character style remains generation context, not another approval call. This is framework-boundary evidence, not a live-model answer-quality claim.

* Real JavaScript, Rust, and Java compiler fixtures exercise task-local, complete-stage, and authoritative commands and retrieve exact process records from PostgreSQL. Built fixture CLIs produce the expected output; missing imports and incompatible types fail before host writes. These checks exposed and fixed a Java projection helper dependency and a reserved JavaScript parameter. Owned compiler output and temporary caches are removed on both paths. JavaScript, Rust, and Java have task-owned behavioral test bodies and real Node/Cargo/JVM execution. Arithmetic and text-transformation fixtures in all three languages pass for correct results and record assertion failures for wrong results before host writes. Test-body context excludes implementation source and declarations. Native Java fixtures also exercise direct dependency projection and explicit failure when assertions are disabled or required input is absent. These constructed fixtures do not establish live-model test quality or functional completion of arbitrary requests.

No learned-skill registry, enrichment, or promotion workflow is currently registered.
Historical skill activity below is not evidence of an available capability.

Container health is a separate live observation; these fixture tests do not
establish the health of a currently deployed service.

None of these statements implies that a raw application request currently completes.

## Counted run 1 — failed baseline

The first frozen black-box run is retained as negative evidence. It is not being
resumed or relabelled after the framework change.

* Job `183`, step `195`; started `2026-07-29 12:30:25 UTC` and stopped at
  `2026-07-29 12:35:34 UTC`.
* Frozen image: `sha256:1734494fa9f5fd8792986c9cfc55e754f43149cbc6d26359287ee15a21555b0d`.
* Fresh workspace: `/home/gryph/Projects/ai/charmander-proof-music-ry4XNA`; initial and final inventories were empty.
* Human or Codex interventions during execution: `0`.
* Result: failed before authoritative workspace writes; runnable applications: `0`.
* Calls: `26`; total prompt characters: `21,104`; maximum prompt characters:
  `3,262`; cumulative model latency: `302,793 ms`.
* Station calls: classification `1`, identity `1`, requirement partition `6`,
  skill procedure `5`, capability relation `10`, fragment generation `1`, and
  fragment correction `2`.
* The first generated declaration contained a placeholder JSX comment. The AST
  gate rejected it. Both correction attempts received the superseded initial
  behavior narrative, retained the comment, and the bounded no-progress guard
  stopped the run after an identical candidate.
* All five learned-skill candidates were rejected after the workflow failure;
  none became active.

This run proved that immutable evidence, exact requirement partitioning,
bounded stations, AST rejection, no-progress detection, retained in-memory work,
and fail-before-write behavior operated. It also proved the correction envelope
was incorrectly broad. The once-proposed whole-declaration correction described by
this historical run is also superseded: the current boundary permits continuation
only after code proves one exact mutable byte span, exposes only that span and one
necessary semantic question to the same persisted job/model context, and splices the
ordinary replacement text into the code-retained base. A new source freeze and empty
workspace are required before run 2.

## Invalidated evidence removed from consideration

Do not count:

* deterministic habit-ledger, unit-converter, or expense-dashboard adapters;
* any workspace Codex edited directly;
* any run given a Codex-authored feature list, decomposition, rubric, source hint, or repair instruction;
* any run resumed after the framework was changed;
* any application-specific helper added to Omnidex after observing a benchmark failure;
* a static blueprint unit test as evidence that semantic interpretation produced that blueprint.

## Required proof run

Before a run starts, record:

* the exact checked-in source used for the run, held fixed until the run stops;
* successful execution of the authoritative fresh-database setup against an empty
  runtime schema;
* exact model routing and embedding identity;
* fresh workspace identity and initial inventory;
* the unchanged user request;
* zero human interventions.

During the run, record actual evidence for:

* each persisted call ID and registered station kind, with the code-owned initial input values;
* exact rendered prompt bytes and raw provider-response bytes;
* exact response bytes;
* every rejection and direct correction;
* every accepted implementation body and code-assembled AST declaration;
* capability-relation decisions and resulting code-owned graph;
* staging and authoritative commands with output, duration, and exit status;
* workspace writes, protected-file reconciliation, final inventory, and stop reason.

After Omnidex stops, a separate observer may evaluate user-visible behavior. The builder must not see that rubric during construction.

## Acceptance sequence

The first counted sequence will use three materially different, fresh junior-application requests. Each request enters unchanged and each run proceeds without intervention. A framework change between attempts invalidates the current workspace and requires a new run from empty state.

For every run, report:

* completed, partial, or failed;
* capabilities demonstrably working and missing;
* stopping failure;
* model calls by station and model;
* total and maximum prompt bytes;
* corrections and retained work;
* verification commands and duration;
* files produced;
* human interventions;
* comparison with the Venusaur negative baseline.

This ledger changes to **proven for the measured scope** only after those artifacts exist. Until then, the correct answer to “is Omnidex usable as an autonomous builder?” is: **the foundation is materially stronger, but autonomy has not yet been demonstrated.**
