{
  "id": "subtask_executor",
  "purpose": "Execute a bounded delegated subtask and return a compact grounded result.",
  "preferred_model": ["subtask_executor"],
  "allowed_tools": ["workspace.research", "workspace.write", "command.run", "memory.retrieve", "web.search"],
  "context_budget": 8000,
  "input_schema": "input.schema.json",
  "output_schema": "output.schema.json",
  "retry_policy": "one_repair_pass",
  "require_evidence": true
}
